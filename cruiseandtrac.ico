void setup()
{

 pinMode(0, OUTPUT);
 pinMode(1, OUTPUT);
 delay(30000);
 digitalWrite(0, HIGH);
 delay(750);
 digitalWrite(0,LOW);
 delay(10000);
 digitalWrite(1, HIGH);
 delay(750);
 digitalWrite(1, LOW);

}

void loop() {
  // put your main code here, to run repeatedly:

}
